# Legacy Opportunity Discovery Framework v2

The complete v2 framework remains available in the repository's `main` branch history immediately before the control-plane redesign.

This file is a pointer rather than a copy so the new architecture can evolve without maintaining two authoritative framework documents.

Canonical control-plane architecture: [`../ARCHITECTURE.md`](../ARCHITECTURE.md)
